# demo: deterministic sample data, no database required.
# postgres: actual Prisma queries; connection failures are surfaced, never hidden.
DATA_SOURCE=demo
# Local development credentials only. Change both values together.
POSTGRES_PASSWORD=cenoved_local_change_me
DATABASE_URL=postgresql://cenoved:cenoved_local_change_me@localhost:5432/cenoved?schema=public
