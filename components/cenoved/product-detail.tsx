'use client';
import Link from 'next/link';
import { useState } from 'react';
import { ArrowRight,ArrowUpRight,ChevronRight,Star,Info,TrendingDown } from 'lucide-react';
import { Tabs,TabsList,TabsTrigger,TabsContent } from '@/components/ui/tabs';
import { Table,TableBody,TableRow,TableHead,TableCell } from '@/components/ui/table';
import { currentPrice,money,dateLabel,formatSpec,priceChange,rating,type Product,type Category } from '@/lib/catalog';
import { CompareButton } from './app-shell';
import { PriceChart } from './price-chart';

export function ProductDetail({product:p,category}:{product:Product;category:Category}) {
 const [tab,setTab]=useState('offers'),[photo,setPhoto]=useState(0),price=currentPrice(p),offers=[...p.offers].sort((a,b)=>Number(b.inStock)-Number(a.inStock)||a.price-b.price),change=priceChange(p);
 function openSection(id:string){setTab(id);setTimeout(()=>document.getElementById('detail-tabs')?.scrollIntoView({behavior:'smooth',block:'start'}),30);}
 const Offers=()=> <section className="white-panel" id="offers"><div className="panel-title"><h2>Предложения магазинов</h2><span>{offers.length} магазина</span></div>{offers.map((o,i)=><div className={'offer-row '+(!o.inStock?'unavailable':'')} key={o.id}><div><div className="offer-store"><span className="shop-avatar">{o.shopName.slice(0,2)}</span><div><strong>{o.shopName}</strong>{i===0&&o.inStock&&<b className="shop-best">Лучшая цена</b>}<span>{o.inStock?o.delivery:'Нет в наличии'}</span></div></div><div className="offer-meta">Обновлено {dateLabel(o.updatedAt)}</div></div><div><div className="offer-price">{money(o.price)}</div><div className="offer-action">{o.inStock?<a href={'/go/'+encodeURIComponent(o.id)} target="_blank" rel="noopener noreferrer nofollow">Перейти в магазин <ArrowUpRight size={14}/></a>:<span className="availability">Недоступно</span>}</div></div></div>)}<p className="history-note">Сортировка по цене товара. Демоссылки ведут на главные страницы магазинов; указанная цена там не подтверждена.</p></section>;
 const specs=<section className="white-panel"><div className="panel-title"><h2>Характеристики</h2></div><p className="description-text">{p.description}</p><Table className="spec-table"><TableBody>{category.attributes.map(a=><TableRow key={a.key}><TableHead scope="row">{a.label}</TableHead><TableCell>{formatSpec(p.specs[a.key],a)}</TableCell></TableRow>)}</TableBody></Table></section>;
 const reviews=<section className="white-panel"><div className="panel-title"><h2>Отзывы покупателей</h2><span>Демонстрационные</span></div><div className="review-grid">{p.reviews.map(r=><article className="review-card" key={r.id}><div><strong>{r.author}</strong><time dateTime={r.date}>{dateLabel(r.date)}</time></div><div className="review-stars" aria-label={`${r.rating} из 5`}>{Array.from({length:5},(_,i)=><Star key={i} size={14} fill={i<r.rating?'currentColor':'none'}/>)}</div><p>{r.text}</p></article>)}</div></section>;
 return <main id="main" className="container detail-main"><div className="breadcrumb"><Link href="/">Главная</Link><ChevronRight size={13}/><Link href={'/category/'+category.slug}>{category.name}</Link><ChevronRight size={13}/><span>{p.brand}</span></div><div className="detail-heading"><div><h1>{p.name}</h1><div className="product-subline"><span className="rating"><Star size={15} fill="currentColor"/>{rating(p).toFixed(1)}</span><button className="text-button" onClick={()=>openSection('reviews')}>{p.reviews.length} демоотзыва</button><span>·</span><span>{p.brand}</span><span>·</span><span>Код: {p.id}</span></div></div><CompareButton product={p}/></div><div className="detail-overview"><div className="product-gallery"><img className="main-product-image" src={p.images[photo]} alt={p.name+' — фото '+(photo+1)} width="600" height="400"/><div className="gallery-thumbs">{p.images.map((src,i)=><button aria-label={'Показать фото '+(i+1)} aria-pressed={photo===i} key={src} className={photo===i?'active':''} onClick={()=>setPhoto(i)}><img src={src} alt=""/></button>)}</div><p className="gallery-caption">{p.imageCaption}</p></div><section className="quick-specs"><h2>Главное о модели</h2><dl>{category.attributes.slice(0,6).map(a=><div className="spec-line" key={a.key}><dt>{a.label}</dt><dd>{formatSpec(p.specs[a.key],a)}</dd></div>)}</dl><button className="inline-link" onClick={()=>openSection('specs')}>Все характеристики <ArrowRight size={14}/></button></section><aside className="price-summary"><span className="summary-label">Лучшая цена из {offers.filter(o=>o.inStock).length} предложений</span><strong className="big-price">{price!==null?money(price):'Нет в наличии'}</strong><div className="history-mini"><TrendingDown size={17}/>{change<0?`На ${Math.abs(change)}% ниже, чем 90 дней назад`:`Изменение за 90 дней: ${change}%`}</div><button className="primary-button" onClick={()=>openSection('offers')}>Сравнить предложения <ArrowRight size={16}/></button><p>Цена без доставки. Наличие и окончательную стоимость уточняйте у продавца.</p></aside></div><div className="demo-notice"><Info size={16}/><span>Демоверсия: цены, комплектации, наличие, история и отзывы сгенерированы. Реальные магазины пока не передают данные в ЦеноВед.</span></div><Tabs id="detail-tabs" value={tab} onValueChange={setTab} className="detail-tabs"><TabsList className="detail-tab-list" variant="line"><TabsTrigger value="offers">Цены <span style={{color:'#929bac',marginLeft:3}}>{offers.length}</span></TabsTrigger><TabsTrigger value="specs">Характеристики</TabsTrigger><TabsTrigger value="history">История цены</TabsTrigger><TabsTrigger value="reviews">Отзывы {p.reviews.length}</TabsTrigger></TabsList><TabsContent value="offers"><div className="detail-content-grid"><Offers/><section className="white-panel"><PriceChart history={p.history}/></section></div></TabsContent><TabsContent value="specs">{specs}</TabsContent><TabsContent value="history"><section className="white-panel" style={{maxWidth:900}}><PriceChart history={p.history}/></section></TabsContent><TabsContent value="reviews">{reviews}</TabsContent></Tabs></main>;
}
