# Проверки поставки — 2026-09-12

Проверено после добавления сообщений о неточной цене:

- Prisma schema format/validate/generate — успешно; добавлена миграция `20260911193000_price_report`.
- `pnpm typecheck` — без ошибок.
- `pnpm lint` — завершён успешно: 0 ошибок, 11 предупреждений по прежним изображениям и зависимостям эффектов.
- `pnpm test` — 30 из 30 проверок прошли. Тесты сообщений покрывают UUID/offer ID, допустимые причины, обязательную цену при расхождении, лишние поля, лимит тела API и честный отказ в demo-режиме. Тесты операторских команд покрывают значения по умолчанию, границы `--limit`, фильтры, безопасные идентификаторы, обязательную однострочную причину и повторные/неизвестные флаги. Инспекция `REVIEW` отдельно проверена на запрет сопоставления по похожему названию и явное отображение несовместимого варианта.
- `pnpm build:next` — production-сборка завершилась успешно; новый маршрут `/api/price-reports` распознан как динамический.
- Страница товара больше не создаёт вложенный React-компонент во время render; устранены три прежние lint-ошибки синхронного setState в эффектах.

Не проверено: применение новой миграции и запись `PriceReport` в живой PostgreSQL — Docker/PostgreSQL в среде отсутствуют. UI-кнопка намеренно видна только при наличии фактического предложения из фида, которого в demo seed нет.

## Предыдущая проверка — 2026-09-11

Проверено после добавления CSV importer v1:

- `prisma validate` и `prisma generate`: схема валидна, Prisma Client 6.19 сгенерирован.
- `pnpm typecheck`: без ошибок.
- `pnpm test`: 16 из 16 проверок прошли. Добавлены проверки CSV/UTF-8-контракта, сохранения ведущего нуля GTIN, точного matching, конфликта варианта, неверной цены/валюты/домена, будущего времени наблюдения, full/incremental semantics, исключения stale из минимума и redirect.
- `pnpm feed:import -- --file fixtures/merchant-feed-v1.csv --mode full --dry-run --catalog fixtures/product-catalog-v1.json --fetched-at 2026-09-11T12:05:00+03:00`: `total=1`, `accepted=1`, `matched=1`, `rejected=0`, `review=0`, `missing=0`.
- `pnpm build:next`: production-сборка Next.js 16.2.6 завершилась успешно; распознаны главная, категории, товары, сравнение и redirect.
- `pnpm lint --quiet`: не прошёл из-за четырёх существующих ошибок новых правил React ESLint в `app-shell.tsx`, `catalog.tsx`, `comparison.tsx` и `product-detail.tsx`. В файлах нового importer ошибок lint не показано. Эти ошибки не блокируют typecheck или production build, но остаются техническим долгом.

Не проверено в этой среде:

- Docker/PostgreSQL отсутствуют. Новая миграция не применялась к живому серверу; транзакционная запись, повторный DB-import, фактическая идемпотентность записей и rollback при ошибке остаются непроверенными end-to-end.
- Реального разрешённого фида и проверенного master-каталога MPN/GTIN нет. `synthetic-shop`, CSV и каталог-кандидат являются тестовыми фикстурами.
- Интерфейс ручного разбора `REVIEW`, расписание импорта и форма сообщения о неверной цене ещё не реализованы.

## Предыдущая проверка — 2026-09-10

Проверено:

- `node --import tsx --test tests/catalog.test.ts tests/redirect.test.ts`: 8 проверок прошли.
- `tsc --noEmit`: без ошибок типов.
- `prisma generate`: Prisma Client 6.19 успешно сгенерирован из схемы проекта.
- `prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script`: исходная PostgreSQL-миграция сгенерирована и просмотрена; добавлены CHECK-ограничения цены, валюты и рейтинга.
- `next build --webpack`: production-сборка Next.js 16.2.6 завершилась успешно; главная, категории, товары, сравнение и переходы распознаны как серверные динамические маршруты.
- Отдельная сборка Sites/Vinext: клиент, SSR и Cloudflare Worker собраны успешно.
- Локальные ссылки всех 36 фотографий в 18 карточках разрешаются в 16 существующих WebP-файлов (фото серий переиспользуются между комплектациями).

Что не проверено в этой среде:

- Применение миграции к запущенному PostgreSQL, выполнение seed и запись Click через реальное подключение: PostgreSQL/Docker здесь отсутствуют. Схема, ORM-клиент и код интеграции подготовлены; end-to-end запуск базы не заявляется как пройденный.
- Интерактивная проверка в браузере, мобильные устройства и реальная клавиатурная навигация. Адаптивные стили и доступные примитивы включены, но это не заменяет браузерную проверку.
- WebMCP: инструмент `filter_catalog` добавлен с проверкой входных данных и обработкой отсутствующего API. Разрешённый поддерживаемый контекст для проверки регистрации и вызова отсутствовал. Обычные элементы управления от него не зависят.
- Реальная цена, доставка, наличие, отзывы, характеристики конкретных SKU и партнёрские ссылки магазинов. Все эти данные демонстрационные.

Следующий обязательный технический шаг перед подключением фидов: запустить PostgreSQL по README, применить миграцию, дважды выполнить seed, убедиться в отсутствии дублей и проверить сохранение Click.
